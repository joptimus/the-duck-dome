# UI Phase 3 Implementation Plan

> **Date:** 2026-03-29
> **PRs:** UI-6, UI-7, UI-8b, UI-8c, UI-9, UI-11, UI-12, UI-13
> **Design source:** `agentchattr/desktop/docs/the-duckdome.jsx`
> **Design spec:** `docs/duckdome-design-spec.md`
> **PR plan:** `docs/migration/ui-pr-plan.md`

---

## Architecture

- **Component pattern:** JSX + CSS Modules (`.module.css`)
- **Token usage:** All colors via `var(--token)`, fonts via `var(--font-*)`, spacing via `var(--ls-*)`, sizes via `var(--fs-*)`
- **State:** React `useState`/`useEffect` hooks. No global state library yet.
- **Icons:** Import named exports from `apps/web/src/components/icons/Icons.jsx`, `BoltIcon.jsx`, `AgentLogo.jsx`
- **Shared primitives:** Import from `apps/web/src/components/shared/` (Dot, Waveform, SectionLabel, ToolbarBtn, CodeTag, StatusTag, RoleTag)
- **Agent data:** Import from `apps/web/src/constants/agents.js` (agentMeta, allAgentTypes)
- **Effects:** Import ElectricPulse from `apps/web/src/components/effects/ElectricPulse.jsx`

### Existing files (already implemented in Phases 1-2)

```
apps/web/src/tokens/colors.css          -- CSS custom properties
apps/web/src/tokens/typography.css       -- font imports + vars
apps/web/src/tokens/animations.css       -- 16 keyframe animations
apps/web/src/index.css                   -- global resets, imports tokens
apps/web/src/components/effects/ElectricPulse.jsx + .module.css
apps/web/src/components/effects/ParticleField.jsx
apps/web/src/components/effects/AmbientOrbs.jsx + .module.css
apps/web/src/components/modals/Modal.jsx + .module.css
apps/web/src/components/modals/SessionLauncher.jsx + .module.css
apps/web/src/components/modals/ScheduleModal.jsx
```

### Assumed to exist from Phase 2 (PRs UI-2, UI-3, UI-5)

These are prerequisites. If they do not exist yet, they must be created first:

```
apps/web/src/components/icons/Icons.jsx       -- 30 named icon exports
apps/web/src/components/icons/BoltIcon.jsx     -- BoltIcon with glow filter
apps/web/src/components/icons/AgentLogo.jsx    -- 7 agent SVG marks + fallback
apps/web/src/components/shared/Dot.jsx         -- Pulsing colored dot
apps/web/src/components/shared/Waveform.jsx    -- Animated bar visualizer
apps/web/src/components/shared/SectionLabel.jsx -- Uppercase label with dot prefix
apps/web/src/components/shared/ToolbarBtn.jsx  -- 32x32 icon button
apps/web/src/components/shared/CodeTag.jsx     -- Inline code pill
apps/web/src/components/shared/StatusTag.jsx   -- Agent status badge
apps/web/src/components/shared/RoleTag.jsx     -- Purple role label pill
apps/web/src/constants/agents.js               -- agentMeta + allAgentTypes
apps/web/src/components/layout/AppShell.jsx    -- 3-zone flex layout
```

---

## PR UI-6: Sidebar

**Branch:** `ui/sidebar`
**Depends on:** UI-2, UI-3, UI-5
**Files touched:** 5 new (4 JSX + 4 CSS Modules), 0 modified

---

### Task 6.1 -- Create `apps/web/src/components/sidebar/Sidebar.jsx`

The main sidebar container with 5 vertical zones: logo header, pinned drawer, channels, repos, user footer.

**File:** `apps/web/src/components/sidebar/Sidebar.jsx`

```jsx
import { useState } from 'react';
import { BoltIcon } from '../icons/BoltIcon';
import { SectionLabel } from '../shared/SectionLabel';
import { ChannelList } from './ChannelList';
import { RepoList } from './RepoList';
import { SidebarFooter } from './SidebarFooter';
import { ChevronIcon } from '../icons/Icons';
import styles from './Sidebar.module.css';

export function Sidebar({
  channels,
  repos,
  activeChannel,
  onSelectChannel,
  onCreateChannel,
  onDeleteChannel,
  onSessionLaunch,
  userName = "James",
  userInitial = "J",
}) {
  const [pinnedOpen, setPinnedOpen] = useState(false);

  return (
    <div className={styles.sidebar}>
      {/* Zone 1: Logo Header */}
      <div className={styles.logoHeader}>
        <div className={styles.gradientLine} />
        <div className={styles.logoIcon}>
          <BoltIcon size={18} color="var(--blue)" />
        </div>
        <div className={styles.logoText}>
          <div className={styles.logoTitle}>THE DUCKDOME</div>
          <div className={styles.logoSubtitle}>AI AGENT BATTLEGROUND</div>
        </div>
      </div>

      {/* Zone 2: Pinned Drawer */}
      <div className={styles.pinnedSection}>
        <div
          className={styles.pinnedHeader}
          onClick={() => setPinnedOpen(!pinnedOpen)}
        >
          <span
            className={styles.pinnedChevron}
            style={{ transform: pinnedOpen ? 'rotate(90deg)' : 'none' }}
          >
            &#9654;
          </span>
          <span className={styles.pinnedLabel}>Pinned</span>
        </div>
        {pinnedOpen && (
          <div className={styles.pinnedEmpty}>No pinned messages</div>
        )}
      </div>

      {/* Zone 3 + 4: Channels + Repos (scrollable) */}
      <div className={styles.scrollArea}>
        {/* Channels */}
        <div className={styles.sectionHeader}>
          <SectionLabel>Channels</SectionLabel>
          <button className={styles.addBtn} onClick={onCreateChannel}>+</button>
        </div>
        <ChannelList
          channels={channels}
          activeChannel={activeChannel}
          onSelect={onSelectChannel}
          onDelete={onDeleteChannel}
        />

        {/* Separator */}
        <div className={styles.separator} />

        {/* Repos */}
        <RepoList repos={repos} />
      </div>

      {/* Zone 5: User Footer */}
      <SidebarFooter
        userName={userName}
        userInitial={userInitial}
        onSessionLaunch={onSessionLaunch}
      />
    </div>
  );
}
```

**File:** `apps/web/src/components/sidebar/Sidebar.module.css`

```css
.sidebar {
  width: 228px;
  min-width: 228px;
  background: rgba(11, 16, 32, 0.94); /* --bg-sidebar at F0 opacity */
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  height: 100%;
  position: relative;
  z-index: 2;
  backdrop-filter: blur(12px);
}

/* ── Logo Header ── */
.logoHeader {
  padding: 14px 16px 10px;
  display: flex;
  align-items: center;
  gap: 10px;
  border-bottom: 1px solid var(--border);
  position: relative;
}

.gradientLine {
  position: absolute;
  bottom: -1px;
  left: 0;
  right: 0;
  height: 2px;
  background: var(--gradient);
  opacity: 0.4;
}

.logoIcon {
  width: 32px;
  height: 32px;
  border-radius: 8px;
  background: linear-gradient(135deg, rgba(0, 212, 255, 0.19), rgba(168, 85, 247, 0.19));
  border: 1.5px solid rgba(0, 212, 255, 0.31);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 0 16px rgba(0, 212, 255, 0.21), 0 0 32px rgba(168, 85, 247, 0.13);
  animation: breathe 3s ease-in-out infinite;
  flex-shrink: 0;
}

.logoTitle {
  font-family: var(--font-display);
  font-weight: 800;
  font-size: 12.5px;
  background: var(--gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  letter-spacing: 0.06em;
  animation: voltFlash 3s ease-in-out infinite;
}

.logoSubtitle {
  font-family: var(--font-label);
  font-size: 8.5px;
  letter-spacing: var(--ls-xxl);
  color: var(--text-muted);
  text-transform: uppercase;
  margin-top: -1px;
}

.logoText {
  min-width: 0;
}

/* ── Pinned Drawer ── */
.pinnedSection {
  border-bottom: 1px solid var(--border);
}

.pinnedHeader {
  padding: 8px 14px;
  display: flex;
  align-items: center;
  gap: 6px;
  cursor: pointer;
}

.pinnedChevron {
  font-size: 10px;
  color: var(--text-muted);
  transition: transform 0.15s;
}

.pinnedLabel {
  font-family: var(--font-label);
  font-size: 11px;
  font-weight: 600;
  color: var(--text-muted);
  letter-spacing: var(--ls-sm);
}

.pinnedEmpty {
  padding: 0 14px 10px;
  font-family: var(--font-body);
  font-size: var(--fs-xs);
  color: var(--text-muted);
  font-style: italic;
}

/* ── Scrollable Area ── */
.scrollArea {
  flex: 1;
  overflow: auto;
  padding-top: 10px;
}

.sectionHeader {
  padding: 0 14px;
  margin-bottom: 8px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.addBtn {
  background: none;
  border: none;
  color: var(--blue);
  cursor: pointer;
  font-size: 16px;
  line-height: 1;
  padding: 0;
}

/* ── Separator ── */
.separator {
  height: 1px;
  margin: 12px 16px;
  background: linear-gradient(90deg, rgba(168, 85, 247, 0.19), var(--border), transparent);
}
```

---

### Task 6.2 -- Create `apps/web/src/components/sidebar/ChannelList.jsx`

Channel rows with active highlight, gradient left bar, unread badges, and hover delete button.

**File:** `apps/web/src/components/sidebar/ChannelList.jsx`

```jsx
import { useState } from 'react';
import styles from './ChannelList.module.css';

export function ChannelList({ channels, activeChannel, onSelect, onDelete }) {
  return (
    <div>
      {channels.map((ch) => (
        <ChannelRow
          key={ch.name}
          channel={ch}
          active={activeChannel === ch.name}
          onSelect={() => onSelect(ch.name)}
          onDelete={() => onDelete(ch.name)}
        />
      ))}
    </div>
  );
}

function ChannelRow({ channel, active, onSelect, onDelete }) {
  const [hovered, setHovered] = useState(false);

  return (
    <div
      className={`${styles.row} ${active ? styles.active : ''}`}
      onClick={onSelect}
      onMouseEnter={() => setHovered(true)}
      onMouseLeave={() => setHovered(false)}
    >
      {active && <div className={styles.activeBar} />}
      <span className={`${styles.hash} ${active ? styles.hashActive : ''}`}>#</span>
      <span className={`${styles.name} ${active ? styles.nameActive : ''}`}>
        {channel.name}
      </span>
      {channel.unread > 0 && (
        <span className={styles.unreadBadge}>{channel.unread}</span>
      )}
      {hovered && !active && (
        <button
          className={styles.deleteBtn}
          onClick={(e) => {
            e.stopPropagation();
            onDelete();
          }}
        >
          &times;
        </button>
      )}
    </div>
  );
}
```

**File:** `apps/web/src/components/sidebar/ChannelList.module.css`

```css
.row {
  padding: 6px 14px 6px 16px;
  display: flex;
  align-items: center;
  gap: 8px;
  cursor: pointer;
  position: relative;
  transition: all 0.12s;
}

.row:hover {
  background: rgba(0, 212, 255, 0.03);
}

.active {
  background: rgba(0, 212, 255, 0.047); /* blue at 0C opacity */
}

.activeBar {
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 2px;
  background: var(--gradient);
}

.hash {
  font-family: var(--font-mono);
  font-size: var(--fs-xs);
  color: var(--text-muted);
}

.hashActive {
  color: var(--blue);
}

.name {
  font-family: var(--font-body);
  font-size: 13.5px;
  font-weight: 400;
  color: var(--text-secondary);
  flex: 1;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.nameActive {
  font-weight: 600;
  color: var(--text-bright);
}

.unreadBadge {
  min-width: 18px;
  height: 18px;
  border-radius: 9px;
  background: linear-gradient(135deg, var(--purple), var(--error));
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 10px;
  font-weight: 700;
  color: #fff;
  font-family: var(--font-label);
  box-shadow: 0 0 10px rgba(168, 85, 247, 0.38);
  padding: 0 5px;
  animation: pulse 1.5s ease-in-out infinite;
}

.deleteBtn {
  background: none;
  border: none;
  color: var(--text-muted);
  cursor: pointer;
  font-size: 14px;
  padding: 0 2px;
  line-height: 1;
}

.deleteBtn:hover {
  color: var(--error);
}
```

---

### Task 6.3 -- Create `apps/web/src/components/sidebar/RepoList.jsx`

Repos section with folder/plus/refresh icons in header and cube icon per repo row.

**File:** `apps/web/src/components/sidebar/RepoList.jsx`

```jsx
import { SectionLabel } from '../shared/SectionLabel';
import { FolderIcon, PlusIcon, RefreshIcon, CubeIcon } from '../icons/Icons';
import styles from './RepoList.module.css';

export function RepoList({ repos }) {
  return (
    <div>
      <div className={styles.header}>
        <SectionLabel color="var(--purple)">Repos</SectionLabel>
        <div className={styles.headerIcons}>
          <span className={styles.headerIcon}>
            <FolderIcon size={12} color="var(--text-muted)" />
          </span>
          <span className={styles.headerIcon}>
            <PlusIcon size={12} color="var(--purple)" />
          </span>
          <span className={styles.headerIcon}>
            <RefreshIcon size={12} color="var(--purple)" />
          </span>
        </div>
      </div>
      {repos.map((repo) => (
        <div key={repo} className={styles.row}>
          <CubeIcon size={11} color="var(--text-muted)" />
          <span className={styles.repoName}>{repo}</span>
        </div>
      ))}
    </div>
  );
}
```

**File:** `apps/web/src/components/sidebar/RepoList.module.css`

```css
.header {
  padding: 0 14px;
  margin-bottom: 8px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.headerIcons {
  display: flex;
  gap: 6px;
  align-items: center;
}

.headerIcon {
  cursor: pointer;
  opacity: 0.5;
  display: flex;
}

.headerIcon:hover {
  opacity: 0.8;
}

.row {
  padding: 5px 14px 5px 16px;
  display: flex;
  align-items: center;
  gap: 7px;
  cursor: pointer;
}

.row:hover {
  background: rgba(255, 255, 255, 0.02);
}

.repoName {
  font-family: var(--font-body);
  font-size: 12.5px;
  color: var(--text-secondary);
  flex: 1;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
```

---

### Task 6.4 -- Create `apps/web/src/components/sidebar/SidebarFooter.jsx`

User avatar with gradient background, name, and session launcher play button.

**File:** `apps/web/src/components/sidebar/SidebarFooter.jsx`

```jsx
import { PlayIcon } from '../icons/Icons';
import styles from './SidebarFooter.module.css';

export function SidebarFooter({ userName, userInitial, onSessionLaunch }) {
  return (
    <div className={styles.footer}>
      <div className={styles.avatar}>{userInitial}</div>
      <span className={styles.userName}>{userName}</span>
      <button className={styles.sessionBtn} onClick={onSessionLaunch}>
        <PlayIcon size={10} color="#fff" />
      </button>
    </div>
  );
}
```

**File:** `apps/web/src/components/sidebar/SidebarFooter.module.css`

```css
.footer {
  padding: 10px 16px;
  border-top: 1px solid var(--border);
  display: flex;
  align-items: center;
  gap: 8px;
}

.avatar {
  width: 24px;
  height: 24px;
  border-radius: 50%;
  background: linear-gradient(135deg, rgba(0, 212, 255, 0.22), rgba(168, 85, 247, 0.19));
  border: 1.5px solid rgba(0, 212, 255, 0.25);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 11px;
  color: var(--blue);
  font-weight: 700;
  font-family: var(--font-display);
  box-shadow: 0 0 10px rgba(0, 212, 255, 0.16);
  flex-shrink: 0;
}

.userName {
  font-family: var(--font-body);
  font-weight: 500;
  font-size: 13px;
  color: var(--text-secondary);
}

.sessionBtn {
  margin-left: auto;
  width: 26px;
  height: 26px;
  border-radius: 50%;
  background: var(--gradient);
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  box-shadow: 0 0 10px rgba(0, 212, 255, 0.25), 0 0 20px rgba(168, 85, 247, 0.13);
  flex-shrink: 0;
}

.sessionBtn:hover {
  box-shadow: 0 0 16px rgba(0, 212, 255, 0.4), 0 0 28px rgba(168, 85, 247, 0.2);
}
```

---

## PR UI-7: Top Bar

**Branch:** `ui/topbar`
**Depends on:** UI-2, UI-3, UI-4, UI-5
**Files touched:** 3 new (1 JSX + 1 CSS Module + 1 PendingApprovalPill)

---

### Task 7.1 -- Create `apps/web/src/components/topbar/TopBar.jsx`

50px bar with BoltIcon, channel name, Waveform, agent status strip, PendingApprovalPill, 7 toolbar buttons, ElectricPulse on bottom edge.

**File:** `apps/web/src/components/topbar/TopBar.jsx`

```jsx
import { BoltIcon } from '../icons/BoltIcon';
import { Waveform } from '../shared/Waveform';
import { ToolbarBtn } from '../shared/ToolbarBtn';
import { AgentLogo } from '../icons/AgentLogo';
import {
  UsersIcon, JobsIcon, RulesIcon, PinIcon, GearIcon, HelpIcon,
} from '../icons/Icons';
import { ElectricPulse } from '../effects/ElectricPulse';
import { PendingApprovalPill } from '../chat/PendingApprovalPill';
import { agentMeta } from '../../constants/agents';
import styles from './TopBar.module.css';

export function TopBar({
  channelName,
  channelAgents = [],
  pendingCount = 0,
  activePanel,
  onTogglePanel,
  onTogglePinned,
}) {
  return (
    <div className={styles.bar}>
      <ElectricPulse vertical={false} color="#A855F7" minDelay={5000} maxDelay={12000} />

      {/* Left: bolt + channel name + waveform */}
      <BoltIcon size={14} color="var(--blue)" />
      <span className={styles.channelName}>{channelName}</span>
      <div className={styles.waveformWrap}>
        <Waveform color="var(--blue)" bars={12} />
      </div>

      <div className={styles.spacer} />

      {/* Agent status strip */}
      <div
        className={styles.agentStrip}
        style={{
          borderColor: activePanel === 'activity'
            ? 'rgba(0, 212, 255, 0.25)'
            : undefined,
        }}
        onClick={() => onTogglePanel('activity')}
      >
        {channelAgents.map((ag) => {
          const meta = agentMeta[ag.agent];
          if (!meta) return null;
          return (
            <div key={ag.agent} className={styles.agentChip}>
              <div className={styles.agentLogoWrap}>
                <AgentLogo agent={ag.agent} size={14} />
                <div
                  className={styles.statusDot}
                  style={{
                    background: ag.running ? 'var(--success)' : 'var(--text-muted)',
                    boxShadow: ag.running ? '0 0 4px var(--success)' : 'none',
                  }}
                />
              </div>
              <span
                className={styles.agentName}
                style={{ color: ag.running ? 'var(--text-secondary)' : 'var(--text-muted)' }}
              >
                {meta.label}
              </span>
            </div>
          );
        })}
      </div>

      {/* Pending approval pill */}
      <PendingApprovalPill count={pendingCount} />

      {/* Toolbar buttons */}
      <div className={styles.toolbar}>
        <ToolbarBtn
          title="Activity"
          active={activePanel === 'activity'}
          onClick={() => onTogglePanel('activity')}
        >
          <BoltIcon size={14} color="currentColor" glow={false} />
        </ToolbarBtn>
        <ToolbarBtn
          title="Agents"
          active={activePanel === 'agents'}
          onClick={() => onTogglePanel('agents')}
        >
          <UsersIcon size={14} color="currentColor" />
        </ToolbarBtn>
        <ToolbarBtn
          title="Jobs"
          active={activePanel === 'jobs'}
          onClick={() => onTogglePanel('jobs')}
        >
          <JobsIcon size={14} color="currentColor" />
        </ToolbarBtn>
        <ToolbarBtn
          title="Rules"
          active={activePanel === 'rules'}
          onClick={() => onTogglePanel('rules')}
        >
          <RulesIcon size={14} color="currentColor" />
        </ToolbarBtn>
        <ToolbarBtn title="Pinned" onClick={onTogglePinned}>
          <PinIcon size={14} color="currentColor" />
        </ToolbarBtn>
        <ToolbarBtn
          title="Settings"
          active={activePanel === 'settings'}
          onClick={() => onTogglePanel('settings')}
        >
          <GearIcon size={14} color="currentColor" />
        </ToolbarBtn>
        <ToolbarBtn title="Help">
          <HelpIcon size={14} color="currentColor" />
        </ToolbarBtn>
      </div>
    </div>
  );
}
```

**File:** `apps/web/src/components/topbar/TopBar.module.css`

```css
.bar {
  height: 50px;
  display: flex;
  align-items: center;
  padding: 0 18px;
  gap: 10px;
  border-bottom: 1px solid var(--border);
  background: rgba(17, 24, 39, 0.82); /* bg-surface at D0 opacity */
  backdrop-filter: blur(12px);
  flex-shrink: 0;
  position: relative;
}

.channelName {
  font-family: var(--font-display);
  font-weight: 600;
  font-size: 13px;
  color: var(--text-bright);
  letter-spacing: var(--ls-xs);
}

.waveformWrap {
  margin-left: 8px;
}

.spacer {
  flex: 1;
}

/* ── Agent Status Strip ── */
.agentStrip {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 5px 14px;
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.012);
  border: 1px solid var(--border);
  animation: borderGlow 6s ease-in-out infinite;
  cursor: pointer;
  transition: all 0.15s;
}

.agentStrip:hover {
  background: rgba(255, 255, 255, 0.025);
}

.agentChip {
  display: flex;
  align-items: center;
  gap: 5px;
}

.agentLogoWrap {
  position: relative;
  display: flex;
}

.statusDot {
  position: absolute;
  bottom: -1px;
  right: -1px;
  width: 5px;
  height: 5px;
  border-radius: 50%;
  border: 1.5px solid var(--bg-surface);
}

.agentName {
  font-family: var(--font-body);
  font-size: 11.5px;
}

/* ── Toolbar ── */
.toolbar {
  display: flex;
  gap: 3px;
  margin-left: 6px;
}
```

---

### Task 7.2 -- Create `apps/web/src/components/chat/PendingApprovalPill.jsx`

Count badge shown in the top bar between agent strip and toolbar. Returns null when count is 0.

**File:** `apps/web/src/components/chat/PendingApprovalPill.jsx`

```jsx
import { ShieldIcon } from '../icons/Icons';
import styles from './PendingApprovalPill.module.css';

export function PendingApprovalPill({ count }) {
  if (!count) return null;

  return (
    <div className={styles.pill}>
      <ShieldIcon size={12} color="var(--warning)" />
      <span className={styles.label}>{count} PENDING</span>
    </div>
  );
}
```

**File:** `apps/web/src/components/chat/PendingApprovalPill.module.css`

```css
.pill {
  display: flex;
  align-items: center;
  gap: 5px;
  padding: 4px 10px 4px 8px;
  border-radius: 8px;
  background: rgba(245, 158, 11, 0.07);
  border: 1px solid rgba(245, 158, 11, 0.19);
  cursor: pointer;
  animation: pulse 1.5s ease-in-out infinite;
}

.label {
  font-family: var(--font-label);
  font-weight: 700;
  font-size: 11px;
  color: var(--warning);
  letter-spacing: var(--ls-xs);
}
```

---

## PR UI-8b: System Messages & Date Dividers

**Branch:** `ui/system-messages`
**Depends on:** UI-2, UI-3
**Files touched:** 2 new (2 JSX + 2 CSS Modules)

---

### Task 8b.1 -- Create `apps/web/src/components/chat/SystemMessage.jsx`

Three variants: join/leave (compact centered line), error (tinted bar with warning triangle), info (centered with BoltIcon).

**File:** `apps/web/src/components/chat/SystemMessage.jsx`

```jsx
import { Dot } from '../shared/Dot';
import { BoltIcon } from '../icons/BoltIcon';
import { agentMeta } from '../../constants/agents';
import styles from './SystemMessage.module.css';

// Warning triangle SVG (inline, not in icon library)
function WarningTriangle() {
  return (
    <svg width={14} height={14} viewBox="0 0 24 24" fill="none"
      stroke="var(--warning)" strokeWidth={2}
      strokeLinecap="round" strokeLinejoin="round">
      <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
      <line x1="12" y1="9" x2="12" y2="13" />
      <line x1="12" y1="17" x2="12.01" y2="17" />
    </svg>
  );
}

export function SystemMessage({ msg, idx = 0 }) {
  const meta = msg.agent ? agentMeta[msg.agent] : null;
  const agentColor = meta?.color || 'var(--text-muted)';
  const isJoin = msg.subtype === 'join';
  const isLeave = msg.subtype === 'leave';
  const isError = msg.subtype === 'error';

  const animStyle = {
    animation: `fadeUp 0.3s ease ${idx * 0.06}s both`,
  };

  // ── Join / Leave ──
  if (isJoin || isLeave) {
    return (
      <div className={styles.joinLeave} style={animStyle}>
        <Dot color={isJoin ? 'var(--success)' : 'var(--text-muted)'} size={5} />
        <span className={styles.agentName} style={{ color: agentColor }}>
          {meta?.label || msg.agent}
        </span>
        <span className={styles.content}>{msg.content}</span>
        <span className={styles.timestamp}>{msg.time}</span>
      </div>
    );
  }

  // ── Error ──
  if (isError) {
    return (
      <div className={styles.error} style={animStyle}>
        <div className={styles.errorIcon}>
          <WarningTriangle />
        </div>
        <div className={styles.errorBody}>
          <span className={styles.errorText}>
            Agent routing for{' '}
            <span style={{ color: agentColor, fontWeight: 500 }}>
              {meta?.label || msg.agent}
            </span>{' '}
            interrupted — auto-recovered. If agents aren&apos;t responding, try
            sending your message again.
          </span>
        </div>
        <span className={styles.errorTimestamp}>{msg.time}</span>
      </div>
    );
  }

  // ── Info (default) ──
  return (
    <div className={styles.info} style={animStyle}>
      <BoltIcon size={11} color="var(--info)" glow={false} />
      <span className={styles.infoText}>{msg.content}</span>
      <span className={styles.timestamp}>{msg.time}</span>
    </div>
  );
}
```

**File:** `apps/web/src/components/chat/SystemMessage.module.css`

```css
/* ── Join / Leave ── */
.joinLeave {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  padding: 4px 0;
  margin: 2px 0;
}

.agentName {
  font-family: var(--font-body);
  font-size: var(--fs-xs);
  font-weight: 500;
}

.content {
  font-family: var(--font-body);
  font-size: var(--fs-xs);
  color: var(--text-muted);
}

.timestamp {
  font-family: var(--font-mono);
  font-size: 9.5px;
  color: var(--text-muted);
  opacity: 0.5;
}

/* ── Error ── */
.error {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  padding: 8px 14px;
  margin: 6px 0;
  border-radius: 8px;
  background: rgba(245, 158, 11, 0.04);
  border: 1px solid rgba(245, 158, 11, 0.09);
}

.errorIcon {
  margin-top: 2px;
  flex-shrink: 0;
}

.errorBody {
  flex: 1;
}

.errorText {
  font-family: var(--font-body);
  font-size: var(--fs-xs);
  color: var(--text-secondary);
  line-height: 1.5;
}

.errorTimestamp {
  font-family: var(--font-mono);
  font-size: 9.5px;
  color: var(--text-muted);
  opacity: 0.5;
  flex-shrink: 0;
  margin-top: 2px;
}

/* ── Info ── */
.info {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 6px 0;
  margin: 4px 0;
}

.infoText {
  font-family: var(--font-body);
  font-size: var(--fs-xs);
  color: var(--text-secondary);
}
```

---

### Task 8b.2 -- Create `apps/web/src/components/chat/DateDivider.jsx`

Horizontal rule with centered label between gradient lines.

**File:** `apps/web/src/components/chat/DateDivider.jsx`

```jsx
import styles from './DateDivider.module.css';

export function DateDivider({ label }) {
  return (
    <div className={styles.divider}>
      <div className={styles.lineLeft} />
      <span className={styles.label}>{label}</span>
      <div className={styles.lineRight} />
    </div>
  );
}
```

**File:** `apps/web/src/components/chat/DateDivider.module.css`

```css
.divider {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 0;
  margin: 4px 0;
}

.lineLeft {
  flex: 1;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--border));
}

.lineRight {
  flex: 1;
  height: 1px;
  background: linear-gradient(90deg, var(--border), transparent);
}

.label {
  font-family: var(--font-label);
  font-weight: 700;
  font-size: 11px;
  letter-spacing: var(--ls-xl);
  color: var(--text-muted);
  text-transform: uppercase;
  flex-shrink: 0;
}
```

---

## PR UI-8c: Tool Approval Cards

**Branch:** `ui/tool-approval`
**Depends on:** UI-2, UI-3
**Files touched:** 1 new (1 JSX + 1 CSS Module)

Note: PendingApprovalPill was created in Task 7.2 above.

---

### Task 8c.1 -- Create `apps/web/src/components/chat/ToolApprovalCard.jsx`

Inline approval card in the chat timeline. Three states: pending (warning), approved (success), denied (error). Shield icon changes by state. Pending shows approve/deny buttons, auto-approve policy dropdown.

**File:** `apps/web/src/components/chat/ToolApprovalCard.jsx`

```jsx
import { useState } from 'react';
import { AgentLogo } from '../icons/AgentLogo';
import {
  ShieldIcon, ShieldCheckIcon, ShieldXIcon, TerminalIcon,
  CheckIcon, XIcon, EyeIcon,
} from '../icons/Icons';
import { agentMeta } from '../../constants/agents';
import styles from './ToolApprovalCard.module.css';

const STATE_COLORS = {
  pending: 'var(--warning)',
  approved: 'var(--success)',
  denied: 'var(--error)',
};

const STATE_LABELS = {
  pending: 'AWAITING APPROVAL',
  approved: 'APPROVED',
  denied: 'DENIED',
};

function ShieldByState({ status, size = 14, color }) {
  if (status === 'approved') return <ShieldCheckIcon size={size} color={color} />;
  if (status === 'denied') return <ShieldXIcon size={size} color={color} />;
  return <ShieldIcon size={size} color={color} />;
}

export function ToolApprovalCard({ msg, idx = 0, onApprove, onDeny }) {
  const meta = agentMeta[msg.agent] || agentMeta.claude;
  const isPending = msg.status === 'pending';
  const borderColor = STATE_COLORS[msg.status] || STATE_COLORS.pending;
  const statusLabel = STATE_LABELS[msg.status] || STATE_LABELS.pending;
  const [showPolicy, setShowPolicy] = useState(false);

  return (
    <div
      className={styles.wrapper}
      style={{ animation: `fadeUp 0.3s ease ${idx * 0.06}s both` }}
    >
      {/* Avatar */}
      <div className={styles.avatarCol}>
        <div
          className={styles.avatar}
          style={{
            background: `${meta.color}15`,
            borderColor: `${meta.color}50`,
          }}
        >
          <AgentLogo agent={msg.agent} size={18} />
        </div>
      </div>

      {/* Card */}
      <div
        className={styles.card}
        style={{
          background: `${borderColor}06`,
          borderColor: `${borderColor}30`,
        }}
      >
        {/* Top accent bar */}
        <div
          className={styles.accentBar}
          style={{
            background: borderColor,
            opacity: isPending ? 1 : 0.4,
          }}
        />

        {/* Header */}
        <div className={styles.header}>
          <ShieldByState status={msg.status} size={14} color={borderColor} />
          <span className={styles.agentName} style={{ color: meta.color }}>
            {meta.label}
          </span>
          <span
            className={styles.statusBadge}
            style={{
              color: borderColor,
              background: `${borderColor}15`,
              borderColor: `${borderColor}25`,
            }}
          >
            {statusLabel}
          </span>
          <span className={styles.time}>{msg.time}</span>
          {!isPending && msg.resolvedAt && (
            <span className={styles.time}>{msg.resolvedAt}</span>
          )}
        </div>

        {/* Tool + Command */}
        <div className={styles.commandSection}>
          <div className={styles.toolRow}>
            <TerminalIcon size={12} color="var(--text-muted)" />
            <span className={styles.toolName}>{msg.tool}</span>
            {msg.diff && <span className={styles.diff}>{msg.diff}</span>}
          </div>
          <div className={styles.commandBlock}>
            <span className={styles.commandPrefix}>$ </span>
            {msg.command}
          </div>
        </div>

        {/* Reason */}
        <div className={styles.reason}>{msg.reason}</div>

        {/* Actions -- pending only */}
        {isPending && (
          <div className={styles.actions}>
            <div className={styles.mainButtons}>
              <button className={styles.approveBtn} onClick={onApprove}>
                <CheckIcon size={13} color="var(--success)" />
                APPROVE
              </button>
              <button className={styles.denyBtn} onClick={onDeny}>
                <XIcon size={12} color="var(--error)" />
                DENY
              </button>
            </div>
            <div className={styles.secondaryButtons}>
              <button
                className={`${styles.ghostBtn} ${showPolicy ? styles.ghostBtnActive : ''}`}
                onClick={() => setShowPolicy(!showPolicy)}
              >
                <ShieldCheckIcon size={11} color="currentColor" />
                Always approve this tool
              </button>
              <button className={styles.ghostBtn}>
                <EyeIcon size={11} color="currentColor" />
                View context
              </button>
            </div>

            {/* Auto-approve policy dropdown */}
            {showPolicy && (
              <div className={styles.policyDropdown}>
                <div className={styles.policyHeader}>AUTO-APPROVE POLICY</div>
                {[
                  {
                    label: `Always approve ${msg.tool} for ${meta.label}`,
                    desc: `${meta.label} can run ${msg.tool} without asking`,
                  },
                  {
                    label: 'Always approve this exact command',
                    desc: 'Only this specific invocation is pre-approved',
                  },
                  {
                    label: `Approve all tools for ${meta.label}`,
                    desc: 'This agent runs unrestricted — use with caution',
                  },
                ].map((p, i) => (
                  <div key={i} className={styles.policyRow}>
                    <div className={styles.policyLabel}>{p.label}</div>
                    <div className={styles.policyDesc}>{p.desc}</div>
                  </div>
                ))}
              </div>
            )}
          </div>
        )}
      </div>
    </div>
  );
}
```

**File:** `apps/web/src/components/chat/ToolApprovalCard.module.css`

```css
.wrapper {
  display: flex;
  align-items: flex-start;
  margin-bottom: 18px;
}

.avatarCol {
  margin-right: 12px;
  margin-top: 2px;
}

.avatar {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  border: 1.5px solid;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.card {
  flex: 1;
  max-width: 80%;
  border: 1px solid;
  border-radius: 12px;
  overflow: hidden;
  position: relative;
}

.accentBar {
  height: 2px;
  width: 100%;
}

/* ── Header ── */
.header {
  padding: 10px 14px;
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}

.agentName {
  font-family: var(--font-label);
  font-weight: 700;
  font-size: var(--fs-xs);
  text-transform: uppercase;
  letter-spacing: var(--ls-xs);
}

.statusBadge {
  font-family: var(--font-label);
  font-weight: 700;
  font-size: 10px;
  letter-spacing: var(--ls-lg);
  padding: 2px 8px;
  border-radius: 4px;
  border: 1px solid;
}

.time {
  font-family: var(--font-mono);
  font-size: 10px;
  color: var(--text-muted);
  margin-left: auto;
}

/* ── Command Block ── */
.commandSection {
  padding: 0 14px;
  margin-bottom: 10px;
}

.toolRow {
  display: flex;
  align-items: center;
  gap: 6px;
  margin-bottom: 6px;
}

.toolName {
  font-family: var(--font-label);
  font-weight: 600;
  font-size: 11px;
  color: var(--text-secondary);
  text-transform: uppercase;
  letter-spacing: var(--ls-sm);
}

.diff {
  font-family: var(--font-mono);
  font-size: 10px;
  color: var(--success);
}

.commandBlock {
  background: var(--bg-app);
  border: 1px solid var(--border);
  border-radius: 6px;
  padding: 8px 12px;
  font-family: var(--font-mono);
  font-size: 11.5px;
  color: var(--text-primary);
  word-break: break-all;
  line-height: 1.6;
}

.commandPrefix {
  color: var(--text-muted);
  user-select: none;
}

/* ── Reason ── */
.reason {
  padding: 0 14px;
  margin-bottom: 12px;
  font-family: var(--font-body);
  font-size: 12.5px;
  color: var(--text-secondary);
  line-height: 1.5;
}

/* ── Actions ── */
.actions {
  padding: 12px 14px;
  border-top: 1px solid rgba(245, 158, 11, 0.09);
  margin-top: 10px;
}

.mainButtons {
  display: flex;
  gap: 8px;
}

.approveBtn {
  flex: 1;
  padding: 8px 0;
  border-radius: 7px;
  background: rgba(0, 255, 163, 0.09);
  border: 1px solid rgba(0, 255, 163, 0.22);
  color: var(--success);
  font-family: var(--font-label);
  font-weight: 700;
  font-size: var(--fs-xs);
  letter-spacing: var(--ls-sm);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  transition: all 0.15s;
}

.approveBtn:hover {
  background: rgba(0, 255, 163, 0.16);
  border-color: rgba(0, 255, 163, 0.38);
  box-shadow: 0 0 16px rgba(0, 255, 163, 0.19);
}

.denyBtn {
  flex: 1;
  padding: 8px 0;
  border-radius: 7px;
  background: rgba(255, 77, 109, 0.06);
  border: 1px solid rgba(255, 77, 109, 0.16);
  color: var(--error);
  font-family: var(--font-label);
  font-weight: 700;
  font-size: var(--fs-xs);
  letter-spacing: var(--ls-sm);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  transition: all 0.15s;
}

.denyBtn:hover {
  background: rgba(255, 77, 109, 0.13);
  border-color: rgba(255, 77, 109, 0.31);
}

/* ── Secondary Buttons ── */
.secondaryButtons {
  display: flex;
  gap: 6px;
  margin-top: 8px;
  justify-content: space-between;
  align-items: center;
}

.ghostBtn {
  padding: 4px 10px;
  border-radius: 5px;
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text-muted);
  font-family: var(--font-body);
  font-size: 10.5px;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 4px;
  transition: all 0.12s;
}

.ghostBtn:hover {
  border-color: var(--blue);
  color: var(--blue);
}

.ghostBtnActive {
  background: rgba(0, 212, 255, 0.06);
  border-color: rgba(0, 212, 255, 0.25);
  color: var(--blue);
}

/* ── Policy Dropdown ── */
.policyDropdown {
  margin-top: 8px;
  padding: 10px 12px;
  background: var(--bg-elevated);
  border: 1px solid var(--border);
  border-radius: 8px;
}

.policyHeader {
  font-family: var(--font-label);
  font-weight: 700;
  font-size: 10px;
  letter-spacing: var(--ls-lg);
  color: var(--blue);
  text-transform: uppercase;
  margin-bottom: 8px;
}

.policyRow {
  padding: 6px 8px;
  border-radius: 6px;
  cursor: pointer;
  margin-bottom: 4px;
  transition: background 0.12s;
}

.policyRow:last-child {
  margin-bottom: 0;
}

.policyRow:hover {
  background: rgba(0, 212, 255, 0.06);
}

.policyLabel {
  font-family: var(--font-body);
  font-size: var(--fs-xs);
  color: var(--text-primary);
  margin-bottom: 2px;
}

.policyDesc {
  font-family: var(--font-body);
  font-size: 10.5px;
  color: var(--text-muted);
}
```

---

## PR UI-9: Chat Timeline + Thinking Strip

**Branch:** `ui/chat-timeline`
**Depends on:** UI-8a, UI-8b, UI-8c, UI-3
**Files touched:** 3 new (3 JSX + 3 CSS Modules)

---

### Task 9.1 -- Create `apps/web/src/components/chat/ChatTimeline.jsx`

Scrollable message list with type dispatch. Renders date_divider, system, tool_approval, and chat messages. Auto-scrolls to bottom when messages change. Renders the session banner at the top.

**File:** `apps/web/src/components/chat/ChatTimeline.jsx`

```jsx
import { useRef, useEffect } from 'react';
import { DateDivider } from './DateDivider';
import { SystemMessage } from './SystemMessage';
import { ToolApprovalCard } from './ToolApprovalCard';
import { SessionBanner } from './SessionBanner';
import styles from './ChatTimeline.module.css';

export function ChatTimeline({
  messages,
  session,
  renderMessage, // function(msg, idx) => JSX for chat-type messages (MsgBubble)
  onApprove,
  onDeny,
}) {
  const scrollRef = useRef(null);

  // Auto-scroll to bottom when messages change
  useEffect(() => {
    if (scrollRef.current) {
      scrollRef.current.scrollTop = scrollRef.current.scrollHeight;
    }
  }, [messages]);

  return (
    <div ref={scrollRef} className={styles.timeline}>
      {/* Session banner */}
      {session && (
        <SessionBanner
          sessionId={session.id}
          agentCount={session.agentCount}
        />
      )}

      {/* Message dispatch */}
      {messages.map((msg, i) => {
        if (msg.type === 'date_divider') {
          return <DateDivider key={i} label={msg.label} />;
        }
        if (msg.type === 'system') {
          return <SystemMessage key={i} msg={msg} idx={i} />;
        }
        if (msg.type === 'tool_approval') {
          return (
            <ToolApprovalCard
              key={msg.id || i}
              msg={msg}
              idx={i}
              onApprove={() => onApprove?.(msg)}
              onDeny={() => onDeny?.(msg)}
            />
          );
        }
        // Default: chat message (MsgBubble from UI-8a)
        return renderMessage(msg, i);
      })}
    </div>
  );
}
```

**File:** `apps/web/src/components/chat/ChatTimeline.module.css`

```css
.timeline {
  flex: 1;
  overflow: auto;
  padding: 20px 24px;
}
```

---

### Task 9.2 -- Create `apps/web/src/components/chat/SessionBanner.jsx`

Active session indicator displayed at the top of the chat timeline.

**File:** `apps/web/src/components/chat/SessionBanner.jsx`

```jsx
import { BoltIcon } from '../icons/BoltIcon';
import styles from './SessionBanner.module.css';

export function SessionBanner({ sessionId = 'DD-0424-A', agentCount = 3 }) {
  return (
    <div className={styles.banner}>
      <div className={styles.lineLeft} />
      <div className={styles.pill}>
        <BoltIcon size={12} color="var(--success)" />
        <span className={styles.label}>SESSION ACTIVE</span>
        <span className={styles.divider} />
        <span className={styles.sessionId}>{sessionId}</span>
        <span className={styles.divider} />
        <span className={styles.agentCount}>{agentCount} AGENTS</span>
      </div>
      <div className={styles.lineRight} />
    </div>
  );
}
```

**File:** `apps/web/src/components/chat/SessionBanner.module.css`

```css
.banner {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  padding: 8px 0;
  margin-bottom: 20px;
}

.lineLeft {
  height: 1px;
  flex: 1;
  background: linear-gradient(90deg, transparent, var(--border));
}

.lineRight {
  height: 1px;
  flex: 1;
  background: linear-gradient(90deg, var(--border), transparent);
}

.pill {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 5px 16px;
  border-radius: 20px;
  background: rgba(0, 255, 163, 0.03);
  border: 1px solid rgba(0, 255, 163, 0.13);
  box-shadow: 0 0 16px rgba(0, 255, 163, 0.07);
}

.label {
  font-family: var(--font-display);
  font-size: 10px;
  font-weight: 700;
  color: var(--success);
  letter-spacing: var(--ls-lg);
}

.divider {
  width: 1px;
  height: 12px;
  background: rgba(0, 255, 163, 0.13);
}

.sessionId {
  font-family: var(--font-mono);
  font-size: 10px;
  color: var(--text-muted);
}

.agentCount {
  font-family: var(--font-label);
  font-size: 10px;
  font-weight: 600;
  color: var(--warning);
}
```

---

### Task 9.3 -- Create `apps/web/src/components/chat/AgentThinkingStrip.jsx`

Multi-agent thinking indicator strip below the chat timeline. Shows pulsing dots + "{Agent} is {status}..." per thinking agent. Collapses to 0 height when empty.

**File:** `apps/web/src/components/chat/AgentThinkingStrip.jsx`

```jsx
import { Dot } from '../shared/Dot';
import { agentMeta } from '../../constants/agents';
import styles from './AgentThinkingStrip.module.css';

export function AgentThinkingStrip({ agents }) {
  if (!agents || agents.length === 0) return null;

  return (
    <div className={styles.strip}>
      {agents.map((a) => {
        const meta = agentMeta[a.key];
        if (!meta) return null;
        return (
          <div key={a.key} className={styles.agent}>
            <Dot color={meta.color} size={6} />
            <span className={styles.text}>
              <span style={{ color: meta.color, fontWeight: 500 }}>
                {meta.label}
              </span>{' '}
              is {a.status}...
            </span>
          </div>
        );
      })}
    </div>
  );
}
```

**File:** `apps/web/src/components/chat/AgentThinkingStrip.module.css`

```css
.strip {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 6px 20px;
  border-top: 1px solid var(--border);
}

.agent {
  display: flex;
  align-items: center;
  gap: 6px;
}

.text {
  font-family: var(--font-body);
  font-size: var(--fs-xs);
  color: var(--text-muted);
  animation: textPulse 2s ease-in-out infinite;
}
```

---

## PR UI-11: Activity Panel

**Branch:** `ui/activity-panel`
**Depends on:** UI-2, UI-3, UI-4, UI-5
**Files touched:** 1 new (1 JSX + 1 CSS Module)

---

### Task 11.1 -- Create `apps/web/src/components/panels/ActivityPanel.jsx`

Two-view panel: normal view (collapsible agent rows with terminal preview) and full log view (fills panel, back button, pinned action bar). Width: 380px.

**Props:**
- `open` (boolean) - whether panel is visible
- `onClose` (function) - close handler
- `agentActivity` (object) - map of agent key to activity data:
  ```js
  {
    claude: {
      task: "Building SettingsPanel.tsx",
      file: "src/components/panels/SettingsPanel.tsx",
      elapsed: "3m 42s",
      status: "PROCESSING",
      output: [
        { t: "3:42", type: "info", text: "Starting SettingsPanel component..." },
        { t: "3:38", type: "cmd", text: "$ npx tsc --noEmit ..." },
        // ...
      ]
    }
  }
  ```

Output line types map to colors: `info` -> text-secondary, `cmd` -> blue, `ok` -> success, `warn` -> warning, `err` -> error.

**File:** `apps/web/src/components/panels/ActivityPanel.jsx`

```jsx
import { useState } from 'react';
import { BoltIcon } from '../icons/BoltIcon';
import { AgentLogo } from '../icons/AgentLogo';
import { Dot } from '../shared/Dot';
import { StatusTag } from '../shared/StatusTag';
import {
  ChevronIcon, PauseIcon, ShuffleIcon, TerminalIcon,
} from '../icons/Icons';
import { ElectricPulse } from '../effects/ElectricPulse';
import { agentMeta } from '../../constants/agents';
import styles from './ActivityPanel.module.css';

const OUTPUT_COLORS = {
  info: 'var(--text-secondary)',
  cmd: 'var(--blue)',
  ok: 'var(--success)',
  warn: 'var(--warning)',
  err: 'var(--error)',
};

export function ActivityPanel({ open, onClose, agentActivity = {} }) {
  const [expanded, setExpanded] = useState({});
  const [fullLog, setFullLog] = useState(null);

  if (!open) return null;

  const toggle = (agent) => {
    setFullLog(null);
    setExpanded((prev) => ({ ...prev, [agent]: !prev[agent] }));
  };

  const activeCount = Object.keys(agentActivity).length;

  // ═══ FULL LOG VIEW ═══
  if (fullLog) {
    const data = agentActivity[fullLog];
    const meta = agentMeta[fullLog];
    if (!data || !meta) return null;

    return (
      <div className={styles.panel}>
        <div className={styles.accentBar} style={{ background: meta.color, opacity: 0.6 }} />

        {/* Header */}
        <div className={styles.header}>
          <div className={styles.headerLeft}>
            <button className={styles.backBtn} onClick={() => setFullLog(null)}>
              <ChevronIcon size={12} color="var(--text-secondary)" rotation={180} />
            </button>
            <AgentLogo agent={fullLog} size={16} />
            <span className={styles.headerTitle} style={{ color: meta.color }}>
              {meta.label} LOG
            </span>
          </div>
          <button className={styles.closeBtn} onClick={onClose}>&times;</button>
        </div>

        {/* Task info bar */}
        <div className={styles.taskInfoBar} style={{ background: `${meta.color}04` }}>
          <div className={styles.taskInfoRow}>
            <span className={styles.taskName}>{data.task}</span>
            <div className={styles.taskMeta}>
              <StatusTag status={data.status} />
              <span className={styles.elapsed}>{data.elapsed}</span>
            </div>
          </div>
          <div className={styles.taskFile}>{data.file}</div>
        </div>

        {/* Full terminal */}
        <div className={styles.fullTerminal}>
          {data.output.map((line, i) => (
            <div key={i} className={styles.termLine}>
              <span className={styles.termTimestamp}>{line.t}</span>
              <span style={{ color: OUTPUT_COLORS[line.type] || 'var(--text-secondary)', wordBreak: 'break-all' }}>
                {line.text}
              </span>
            </div>
          ))}
        </div>

        {/* Bottom actions */}
        <div className={styles.bottomBar}>
          <button className={styles.actionBtn}>
            <PauseIcon size={11} color="currentColor" />
            Pause
          </button>
          <button className={styles.actionBtn}>
            <ShuffleIcon size={11} color="currentColor" />
            Reassign
          </button>
        </div>
      </div>
    );
  }

  // ═══ NORMAL VIEW ═══
  return (
    <div className={styles.panel}>
      <div className={styles.accentBar} />
      <ElectricPulse vertical color="#A855F7" minDelay={4000} maxDelay={10000} />

      {/* Header */}
      <div className={styles.header}>
        <div className={styles.headerLeft}>
          <BoltIcon size={14} color="var(--blue)" glow={false} />
          <span className={styles.headerTitle}>ACTIVITY</span>
          <span className={styles.activeCount}>{activeCount} active</span>
        </div>
        <button className={styles.closeBtn} onClick={onClose}>&times;</button>
      </div>

      {/* Agent rows */}
      <div className={styles.agentList}>
        {Object.entries(agentActivity).map(([agent, data]) => {
          const meta = agentMeta[agent];
          if (!meta) return null;
          const isOpen = expanded[agent];
          const latestLine = data.output[0];

          return (
            <div key={agent} className={styles.agentRow}>
              {/* Clickable header */}
              <div
                className={styles.agentHeader}
                onClick={() => toggle(agent)}
                style={{ background: isOpen ? `${meta.color}06` : undefined }}
              >
                <ChevronIcon size={10} color={meta.color} rotation={isOpen ? 90 : 0} />
                <Dot color={meta.color} size={7} />
                <span className={styles.agentLabel} style={{ color: meta.color }}>
                  {meta.label}
                </span>
                <StatusTag status={data.status} />
                <span className={styles.elapsed}>{data.elapsed}</span>
              </div>

              {/* Collapsed preview */}
              {!isOpen && latestLine && (
                <div className={styles.preview} onClick={() => toggle(agent)}>
                  <div
                    className={styles.previewText}
                    style={{ color: OUTPUT_COLORS[latestLine.type] || 'var(--text-muted)' }}
                  >
                    {latestLine.text}
                  </div>
                </div>
              )}

              {/* Expanded content */}
              {isOpen && (
                <div className={styles.expandedContent}>
                  {/* Task card */}
                  <div className={styles.taskCard}>
                    <div className={styles.taskCardName}>{data.task}</div>
                    <div className={styles.taskCardFile}>{data.file}</div>
                  </div>

                  {/* Terminal output */}
                  <div className={styles.terminal}>
                    {data.output.map((line, i) => (
                      <div key={i} className={styles.termLine}>
                        <span className={styles.termTimestamp}>{line.t}</span>
                        <span style={{ color: OUTPUT_COLORS[line.type] || 'var(--text-secondary)', wordBreak: 'break-all' }}>
                          {line.text}
                        </span>
                      </div>
                    ))}
                  </div>

                  {/* Quick actions */}
                  <div className={styles.quickActions}>
                    <button className={styles.actionBtn}>
                      <PauseIcon size={11} color="currentColor" />
                      Pause
                    </button>
                    <button className={styles.actionBtn}>
                      <ShuffleIcon size={11} color="currentColor" />
                      Reassign
                    </button>
                    <button
                      className={styles.actionBtn}
                      onClick={() => setFullLog(agent)}
                    >
                      <TerminalIcon size={11} color="currentColor" />
                      Full log
                    </button>
                  </div>
                </div>
              )}
            </div>
          );
        })}
      </div>
    </div>
  );
}
```

**File:** `apps/web/src/components/panels/ActivityPanel.module.css`

```css
.panel {
  width: 380px;
  background: var(--bg-surface);
  border-left: 1px solid var(--border);
  height: 100%;
  display: flex;
  flex-direction: column;
  position: relative;
  flex-shrink: 0;
  animation: slideIn 0.25s ease;
}

.accentBar {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2px;
  background: var(--gradient);
  opacity: 0.6;
}

/* ── Header ── */
.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14px 18px;
  border-bottom: 1px solid var(--border);
}

.headerLeft {
  display: flex;
  align-items: center;
  gap: 6px;
}

.headerTitle {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: var(--fs-xs);
  color: var(--text-bright);
  letter-spacing: var(--ls-sm);
}

.activeCount {
  font-family: var(--font-mono);
  font-size: 10px;
  color: var(--success);
  margin-left: 4px;
}

.closeBtn {
  background: none;
  border: none;
  color: var(--text-muted);
  cursor: pointer;
  font-size: 18px;
  padding: 0;
  line-height: 1;
}

.closeBtn:hover {
  color: var(--text-primary);
}

.backBtn {
  background: none;
  border: none;
  color: var(--text-muted);
  cursor: pointer;
  display: flex;
  padding: 0;
}

/* ── Agent List ── */
.agentList {
  flex: 1;
  overflow: auto;
  padding: 8px 0;
}

.agentRow {
  border-bottom: 1px solid var(--border);
}

.agentHeader {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px 16px;
  cursor: pointer;
  transition: background 0.15s;
}

.agentHeader:hover {
  background: rgba(255, 255, 255, 0.02);
}

.agentLabel {
  font-family: var(--font-label);
  font-weight: 700;
  font-size: 13px;
  text-transform: uppercase;
  letter-spacing: var(--ls-xs);
  flex-shrink: 0;
}

.elapsed {
  margin-left: auto;
  font-family: var(--font-mono);
  font-size: 10px;
  color: var(--text-muted);
  flex-shrink: 0;
}

/* ── Preview (collapsed) ── */
.preview {
  padding: 0 16px 10px 42px;
  cursor: pointer;
}

.previewText {
  font-family: var(--font-mono);
  font-size: 10px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  opacity: 0.7;
}

/* ── Expanded ── */
.expandedContent {
  padding: 0 16px 14px;
}

.taskCard {
  margin-bottom: 10px;
  padding: 8px 12px;
  background: var(--bg-elevated);
  border-radius: 6px;
  border: 1px solid var(--border);
}

.taskCardName {
  font-family: var(--font-body);
  font-size: 12.5px;
  color: var(--text-primary);
  margin-bottom: 3px;
}

.taskCardFile {
  font-family: var(--font-mono);
  font-size: 10px;
  color: var(--text-muted);
  opacity: 0.7;
}

/* ── Terminal ── */
.terminal {
  background: var(--bg-app);
  border: 1px solid var(--border);
  border-radius: 6px;
  max-height: 180px;
  overflow: auto;
  font-family: var(--font-mono);
  font-size: 10.5px;
  line-height: 1.7;
}

.fullTerminal {
  flex: 1;
  overflow: auto;
  background: var(--bg-app);
  font-family: var(--font-mono);
  font-size: 11px;
  line-height: 1.8;
}

.termLine {
  padding: 2px 10px;
  display: flex;
  gap: 8px;
  align-items: flex-start;
  border-bottom: 1px solid rgba(31, 41, 55, 0.13);
  transition: background 0.1s;
}

.termLine:hover {
  background: rgba(255, 255, 255, 0.02);
}

.termTimestamp {
  color: var(--text-muted);
  opacity: 0.4;
  flex-shrink: 0;
  min-width: 30px;
  text-align: right;
  user-select: none;
}

/* ── Quick Actions ── */
.quickActions {
  display: flex;
  gap: 6px;
  margin-top: 8px;
}

.actionBtn {
  padding: 4px 10px;
  border-radius: 5px;
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text-muted);
  font-family: var(--font-body);
  font-size: 10.5px;
  cursor: pointer;
  transition: all 0.12s;
  display: flex;
  align-items: center;
  gap: 4px;
}

.actionBtn:hover {
  border-color: var(--text-secondary);
  color: var(--text-secondary);
}

/* ── Bottom Bar (full log) ── */
.bottomBar {
  padding: 10px 16px;
  border-top: 1px solid var(--border);
  display: flex;
  gap: 6px;
  background: var(--bg-surface);
}

.bottomBar .actionBtn {
  flex: 1;
  padding: 6px 0;
  border-radius: 6px;
  justify-content: center;
  gap: 5px;
  font-size: 11px;
}

/* ── Task Info Bar (full log) ── */
.taskInfoBar {
  padding: 10px 16px;
  border-bottom: 1px solid var(--border);
}

.taskInfoRow {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 4px;
}

.taskName {
  font-family: var(--font-body);
  font-size: 12.5px;
  color: var(--text-primary);
  font-weight: 500;
}

.taskMeta {
  display: flex;
  align-items: center;
  gap: 6px;
}

.taskFile {
  font-family: var(--font-mono);
  font-size: 10px;
  color: var(--text-muted);
  opacity: 0.7;
}
```

---

## PR UI-12: Agents Panel

**Branch:** `ui/agents-panel`
**Depends on:** UI-2, UI-3, UI-4, UI-5
**Files touched:** 1 new (1 JSX + 1 CSS Module)

---

### Task 12.1 -- Create `apps/web/src/components/panels/AgentsPanel.jsx`

Per-channel agent process manager. Width: 360px. Features: agent cards with start/stop, system prompt viewing/editing, add agent flow with type picker.

**Props:**
- `open` (boolean) - whether panel is visible
- `onClose` (function) - close handler
- `channelName` (string) - current channel name
- `agents` (array) - list of agent configs:
  ```js
  [{ agent: "claude", running: true, repo: "agentchattr", pid: 48231, uptime: "32m", prompt: "You are..." }]
  ```
- `repos` (array of strings) - available repo names
- `onToggleAgent` (function(idx)) - start/stop handler
- `onRemoveAgent` (function(idx)) - remove handler
- `onAddAgent` (function({ type, repo, prompt })) - spawn handler

**File:** `apps/web/src/components/panels/AgentsPanel.jsx`

```jsx
import { useState } from 'react';
import { AgentLogo } from '../icons/AgentLogo';
import { Dot } from '../shared/Dot';
import {
  UsersIcon, PlayIcon, StopIcon, FolderIcon, TerminalIcon,
  EditIcon, TrashIcon, PlusIcon, PowerIcon,
} from '../icons/Icons';
import { BoltIcon } from '../icons/BoltIcon';
import { ElectricPulse } from '../effects/ElectricPulse';
import { agentMeta, allAgentTypes } from '../../constants/agents';
import styles from './AgentsPanel.module.css';

export function AgentsPanel({
  open,
  onClose,
  channelName = 'general',
  agents = [],
  repos = [],
  onToggleAgent,
  onRemoveAgent,
  onAddAgent,
}) {
  const [editingPrompt, setEditingPrompt] = useState(null);
  const [adding, setAdding] = useState(false);
  const [newAgent, setNewAgent] = useState({ type: '', repo: '', prompt: '' });

  if (!open) return null;

  const runCount = agents.filter((a) => a.running).length;
  const availableTypes = allAgentTypes.filter(
    (t) => !agents.find((a) => a.agent === t)
  );

  const handleAdd = () => {
    if (!newAgent.type || !newAgent.repo) return;
    onAddAgent?.(newAgent);
    setNewAgent({ type: '', repo: '', prompt: '' });
    setAdding(false);
  };

  return (
    <div className={styles.panel}>
      <div className={styles.accentBar} />
      <ElectricPulse vertical color="#A855F7" minDelay={4000} maxDelay={10000} />

      {/* Header */}
      <div className={styles.header}>
        <div className={styles.headerLeft}>
          <UsersIcon size={14} color="var(--blue)" />
          <span className={styles.headerTitle}>AGENTS</span>
          <span
            className={styles.runCount}
            style={{ color: runCount > 0 ? 'var(--success)' : 'var(--text-muted)' }}
          >
            {runCount} running
          </span>
        </div>
        <button className={styles.closeBtn} onClick={onClose}>&times;</button>
      </div>

      {/* Channel context */}
      <div className={styles.channelBar}>
        <div className={styles.channelLabel}>Channel</div>
        <div className={styles.channelName}>#{channelName}</div>
      </div>

      {/* Agent list */}
      <div className={styles.agentList}>
        {agents.map((ag, idx) => {
          const meta = agentMeta[ag.agent];
          if (!meta) return null;
          const isEditing = editingPrompt === idx;

          return (
            <div
              key={ag.agent}
              className={styles.agentCard}
              style={{ opacity: ag.running ? 1 : 0.6 }}
            >
              {/* Agent header row */}
              <div className={styles.agentHeaderRow}>
                <div
                  className={styles.avatar}
                  style={{
                    background: `${meta.color}15`,
                    borderColor: ag.running ? `${meta.color}50` : 'var(--border)',
                  }}
                >
                  <AgentLogo agent={ag.agent} size={18} />
                </div>
                <div className={styles.agentInfo}>
                  <div className={styles.agentNameRow}>
                    <span className={styles.agentLabel} style={{ color: meta.color }}>
                      {meta.label}
                    </span>
                    <Dot color={ag.running ? 'var(--success)' : 'var(--text-muted)'} size={6} />
                    <span className={styles.statusText}>
                      {ag.running ? 'running' : 'stopped'}
                    </span>
                  </div>
                  {ag.running && (
                    <div className={styles.pidLine}>
                      PID {ag.pid} &middot; up {ag.uptime}
                    </div>
                  )}
                </div>
                {/* Start/Stop button */}
                <button
                  className={`${styles.toggleBtn} ${ag.running ? styles.stopBtn : styles.startBtn}`}
                  onClick={() => onToggleAgent?.(idx)}
                >
                  {ag.running
                    ? <StopIcon size={12} color="var(--error)" />
                    : <PlayIcon size={10} color="var(--success)" />
                  }
                </button>
              </div>

              {/* Repo dropdown */}
              <div className={styles.fieldGroup}>
                <div className={styles.fieldLabel}>
                  <FolderIcon size={10} color="var(--text-muted)" />
                  Working directory
                </div>
                <select className={styles.repoSelect} defaultValue={ag.repo}>
                  {repos.map((r) => (
                    <option key={r} value={r}>{r}</option>
                  ))}
                </select>
              </div>

              {/* System prompt */}
              <div className={styles.fieldGroup}>
                <div className={styles.fieldLabelRow}>
                  <span className={styles.fieldLabel}>
                    <TerminalIcon size={10} color="var(--text-muted)" />
                    System prompt
                  </span>
                  <button
                    className={styles.editBtn}
                    style={{ color: isEditing ? meta.color : 'var(--text-muted)' }}
                    onClick={() => setEditingPrompt(isEditing ? null : idx)}
                  >
                    <EditIcon size={11} color="currentColor" />
                  </button>
                </div>
                {isEditing ? (
                  <textarea
                    className={styles.promptTextarea}
                    defaultValue={ag.prompt}
                    rows={4}
                    style={{ borderColor: `${meta.color}40` }}
                  />
                ) : (
                  <div
                    className={styles.promptCollapsed}
                    onClick={() => setEditingPrompt(idx)}
                  >
                    {ag.prompt}
                    <div className={styles.promptFade} />
                  </div>
                )}
              </div>

              {/* Remove button -- stopped only */}
              {!ag.running && (
                <div className={styles.removeRow}>
                  <button
                    className={styles.removeBtn}
                    onClick={() => onRemoveAgent?.(idx)}
                  >
                    <TrashIcon size={10} color="currentColor" />
                    Remove from channel
                  </button>
                </div>
              )}
            </div>
          );
        })}

        {/* Add agent section */}
        {!adding ? (
          <div className={styles.addSection}>
            <button
              className={styles.addTrigger}
              disabled={availableTypes.length === 0}
              onClick={() => setAdding(true)}
            >
              <PlusIcon size={12} color="currentColor" />
              {availableTypes.length
                ? `Add agent to #${channelName}`
                : 'All agents assigned'}
            </button>
          </div>
        ) : (
          <div className={styles.addForm}>
            <div className={styles.addFormTitle}>Add Agent</div>

            {/* Agent type picker */}
            <div className={styles.fieldLabel}>Agent</div>
            <div className={styles.typePicker}>
              {availableTypes.map((t) => {
                const m = agentMeta[t];
                const sel = newAgent.type === t;
                return (
                  <button
                    key={t}
                    className={`${styles.typeBtn} ${sel ? styles.typeBtnSelected : ''}`}
                    style={{
                      color: sel ? m.color : undefined,
                      background: sel ? `${m.color}15` : undefined,
                      borderColor: sel ? `${m.color}50` : undefined,
                    }}
                    onClick={() => setNewAgent((p) => ({ ...p, type: t }))}
                  >
                    <AgentLogo agent={t} size={14} />
                    {m.label}
                  </button>
                );
              })}
            </div>

            {/* Repo picker */}
            <div className={styles.fieldLabel}>
              <FolderIcon size={10} color="var(--text-muted)" />
              Working directory
            </div>
            <select
              className={styles.repoSelect}
              value={newAgent.repo}
              onChange={(e) => setNewAgent((p) => ({ ...p, repo: e.target.value }))}
            >
              <option value="">Select a repo...</option>
              {repos.map((r) => (
                <option key={r} value={r}>{r}</option>
              ))}
            </select>

            {/* System prompt */}
            <div className={styles.fieldLabel}>
              <TerminalIcon size={10} color="var(--text-muted)" />
              System prompt
            </div>
            <textarea
              className={styles.promptTextarea}
              value={newAgent.prompt}
              onChange={(e) => setNewAgent((p) => ({ ...p, prompt: e.target.value }))}
              rows={3}
              placeholder="Define how this agent should behave..."
            />

            {/* Actions */}
            <div className={styles.addActions}>
              <button
                className={styles.cancelBtn}
                onClick={() => {
                  setAdding(false);
                  setNewAgent({ type: '', repo: '', prompt: '' });
                }}
              >
                Cancel
              </button>
              <button
                className={styles.spawnBtn}
                disabled={!newAgent.type || !newAgent.repo}
                onClick={handleAdd}
              >
                {newAgent.type && newAgent.repo && (
                  <div className={styles.shimmerOverlay} />
                )}
                <span className={styles.spawnContent}>
                  <PowerIcon size={11} color="currentColor" />
                  SPAWN
                </span>
              </button>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
```

**File:** `apps/web/src/components/panels/AgentsPanel.module.css`

```css
.panel {
  width: 360px;
  background: var(--bg-surface);
  border-left: 1px solid var(--border);
  height: 100%;
  overflow: auto;
  display: flex;
  flex-direction: column;
  position: relative;
  flex-shrink: 0;
  animation: slideIn 0.25s ease;
}

.accentBar {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2px;
  background: var(--gradient);
  opacity: 0.6;
}

/* ── Header ── */
.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14px 18px;
  border-bottom: 1px solid var(--border);
}

.headerLeft {
  display: flex;
  align-items: center;
  gap: 6px;
}

.headerTitle {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: var(--fs-xs);
  color: var(--text-bright);
  letter-spacing: var(--ls-sm);
}

.runCount {
  font-family: var(--font-mono);
  font-size: 10px;
  margin-left: 4px;
}

.closeBtn {
  background: none;
  border: none;
  color: var(--text-muted);
  cursor: pointer;
  font-size: 18px;
}

.closeBtn:hover {
  color: var(--text-primary);
}

/* ── Channel Context ── */
.channelBar {
  padding: 10px 18px;
  border-bottom: 1px solid var(--border);
  background: rgba(0, 212, 255, 0.016);
}

.channelLabel {
  font-family: var(--font-body);
  font-size: 11px;
  color: var(--text-muted);
}

.channelName {
  font-family: var(--font-body);
  font-size: 13px;
  color: var(--text-primary);
  font-weight: 500;
}

/* ── Agent List ── */
.agentList {
  flex: 1;
  overflow: auto;
  padding: 8px 0;
}

/* ── Agent Card ── */
.agentCard {
  padding: 14px 16px;
  border-bottom: 1px solid var(--border);
  transition: opacity 0.2s;
}

.agentHeaderRow {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 10px;
}

.avatar {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  border: 1.5px solid;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: border-color 0.2s;
  flex-shrink: 0;
}

.agentInfo {
  flex: 1;
}

.agentNameRow {
  display: flex;
  align-items: center;
  gap: 6px;
}

.agentLabel {
  font-family: var(--font-label);
  font-weight: 700;
  font-size: var(--fs-label);
  text-transform: uppercase;
  letter-spacing: var(--ls-xs);
}

.statusText {
  font-family: var(--font-mono);
  font-size: 10px;
  color: var(--text-muted);
}

.pidLine {
  font-family: var(--font-mono);
  font-size: 10px;
  color: var(--text-muted);
  margin-top: 1px;
}

/* ── Toggle Button ── */
.toggleBtn {
  width: 32px;
  height: 32px;
  border-radius: 8px;
  border: 1px solid;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.15s;
  flex-shrink: 0;
}

.startBtn {
  background: rgba(0, 255, 163, 0.07);
  border-color: rgba(0, 255, 163, 0.19);
}

.startBtn:hover {
  background: rgba(0, 255, 163, 0.16);
  box-shadow: 0 0 12px rgba(0, 255, 163, 0.19);
}

.stopBtn {
  background: rgba(255, 77, 109, 0.07);
  border-color: rgba(255, 77, 109, 0.19);
}

.stopBtn:hover {
  background: rgba(255, 77, 109, 0.16);
  box-shadow: 0 0 12px rgba(255, 77, 109, 0.19);
}

/* ── Field Groups ── */
.fieldGroup {
  margin-bottom: 8px;
}

.fieldLabel {
  font-family: var(--font-body);
  font-size: 10px;
  color: var(--text-muted);
  margin-bottom: 3px;
  display: flex;
  align-items: center;
  gap: 4px;
}

.fieldLabelRow {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 3px;
}

.editBtn {
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
  display: flex;
}

.repoSelect {
  width: 100%;
  padding: 6px 10px;
  background: var(--bg-input);
  border: 1px solid var(--border);
  border-radius: 5px;
  color: var(--text-primary);
  font-family: var(--font-mono);
  font-size: 11px;
  outline: none;
  appearance: none;
  cursor: pointer;
}

.repoSelect:focus {
  border-color: var(--blue);
}

/* ── System Prompt ── */
.promptCollapsed {
  padding: 6px 10px;
  background: var(--bg-app);
  border: 1px solid var(--border);
  border-radius: 5px;
  font-family: var(--font-body);
  font-size: 11px;
  color: var(--text-secondary);
  line-height: 1.5;
  max-height: 42px;
  overflow: hidden;
  position: relative;
  cursor: pointer;
}

.promptFade {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 18px;
  background: linear-gradient(transparent, var(--bg-app));
}

.promptTextarea {
  width: 100%;
  padding: 8px 10px;
  background: var(--bg-input);
  border: 1px solid;
  border-radius: 5px;
  color: var(--text-primary);
  font-family: var(--font-body);
  font-size: 11.5px;
  line-height: 1.5;
  outline: none;
  resize: vertical;
  box-sizing: border-box;
}

/* ── Remove Button ── */
.removeRow {
  margin-top: 8px;
  display: flex;
  justify-content: flex-end;
}

.removeBtn {
  padding: 3px 10px;
  border-radius: 5px;
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text-muted);
  font-family: var(--font-body);
  font-size: 10px;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 4px;
  transition: all 0.12s;
}

.removeBtn:hover {
  border-color: var(--error);
  color: var(--error);
}

/* ── Add Agent Section ── */
.addSection {
  padding: 14px 16px;
}

.addTrigger {
  width: 100%;
  padding: 10px;
  border-radius: 8px;
  background: transparent;
  border: 1px dashed rgba(0, 212, 255, 0.25);
  color: var(--blue);
  font-family: var(--font-body);
  font-size: 12.5px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  transition: all 0.15s;
}

.addTrigger:hover {
  background: rgba(0, 212, 255, 0.03);
  border-color: var(--blue);
}

.addTrigger:disabled {
  color: var(--text-muted);
  border-color: var(--border);
  cursor: default;
}

/* ── Add Form ── */
.addForm {
  padding: 14px 16px;
  border-top: 1px solid var(--border);
}

.addFormTitle {
  font-family: var(--font-label);
  font-weight: 700;
  font-size: 11px;
  letter-spacing: var(--ls-lg);
  color: var(--blue);
  text-transform: uppercase;
  margin-bottom: 10px;
}

.typePicker {
  display: flex;
  gap: 6px;
  margin-bottom: 12px;
}

.typeBtn {
  flex: 1;
  padding: 8px 0;
  border-radius: 7px;
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text-secondary);
  font-family: var(--font-label);
  font-weight: 700;
  font-size: var(--fs-xs);
  letter-spacing: var(--ls-xs);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
  transition: all 0.12s;
  text-transform: uppercase;
}

.typeBtn:hover {
  border-color: var(--text-secondary);
}

.typeBtnSelected {
  /* colors set inline via style prop */
}

/* ── Add Actions ── */
.addActions {
  display: flex;
  gap: 8px;
  margin-top: 12px;
}

.cancelBtn {
  flex: 1;
  padding: 8px 0;
  border-radius: 7px;
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text-secondary);
  font-family: var(--font-body);
  font-size: var(--fs-xs);
  cursor: pointer;
}

.spawnBtn {
  flex: 1;
  padding: 8px 0;
  border-radius: 7px;
  background: var(--gradient);
  border: none;
  color: #fff;
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 11px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
  box-shadow: 0 0 14px rgba(0, 212, 255, 0.25), 0 0 30px rgba(168, 85, 247, 0.13);
  position: relative;
  overflow: hidden;
}

.spawnBtn:disabled {
  background: rgba(255, 255, 255, 0.02);
  color: var(--text-muted);
  box-shadow: none;
  cursor: default;
}

.shimmerOverlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.12), transparent);
  background-size: 200% 100%;
  animation: shimmer 2s ease infinite;
}

.spawnContent {
  position: relative;
  display: flex;
  align-items: center;
  gap: 4px;
}
```

---

## PR UI-13: Jobs, Rules & Settings Panels + RightPanel Wrapper

**Branch:** `ui/right-panels`
**Depends on:** UI-2, UI-3, UI-4, UI-5
**Files touched:** 4 new (4 JSX + 4 CSS Modules)

---

### Task 13.1 -- Create `apps/web/src/components/panels/RightPanel.jsx`

Shared wrapper for Jobs, Rules, Settings. Provides: gradient top bar (2px), ElectricPulse vertical, header with icon + title + close button. Panels pass their own width.

**File:** `apps/web/src/components/panels/RightPanel.jsx`

```jsx
import { ElectricPulse } from '../effects/ElectricPulse';
import styles from './RightPanel.module.css';

export function RightPanel({ open, onClose, title, icon, width = 380, children }) {
  if (!open) return null;

  return (
    <div className={styles.panel} style={{ width }}>
      <div className={styles.accentBar} />
      <ElectricPulse vertical color="#A855F7" minDelay={4000} maxDelay={10000} />
      <div className={styles.header}>
        <div className={styles.headerLeft}>
          {icon}
          <span className={styles.headerTitle}>{title}</span>
        </div>
        <button className={styles.closeBtn} onClick={onClose}>&times;</button>
      </div>
      <div className={styles.body}>{children}</div>
    </div>
  );
}
```

**File:** `apps/web/src/components/panels/RightPanel.module.css`

```css
.panel {
  background: var(--bg-surface);
  border-left: 1px solid var(--border);
  height: 100%;
  overflow: auto;
  animation: slideIn 0.25s ease;
  display: flex;
  flex-direction: column;
  position: relative;
  flex-shrink: 0;
}

.accentBar {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2px;
  background: var(--gradient);
  opacity: 0.6;
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14px 18px;
  border-bottom: 1px solid var(--border);
}

.headerLeft {
  display: flex;
  align-items: center;
  gap: 6px;
}

.headerTitle {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: var(--fs-xs);
  color: var(--text-bright);
  letter-spacing: var(--ls-sm);
}

.closeBtn {
  background: none;
  border: none;
  color: var(--text-muted);
  cursor: pointer;
  font-size: 18px;
}

.closeBtn:hover {
  color: var(--text-primary);
}

.body {
  flex: 1;
  padding: 16px;
}
```

---

### Task 13.2 -- Create `apps/web/src/components/panels/JobsPanel.jsx`

Jobs panel with empty state and 3 status group headers. Width: 380px.

**File:** `apps/web/src/components/panels/JobsPanel.jsx`

```jsx
import { RightPanel } from './RightPanel';
import { JobsIcon } from '../icons/Icons';
import styles from './JobsPanel.module.css';

const STATUS_GROUPS = [
  { label: 'TO DO', color: 'var(--text-muted)' },
  { label: 'ACTIVE', color: 'var(--success)' },
  { label: 'CLOSED', color: 'var(--error)' },
];

export function JobsPanel({ open, onClose }) {
  return (
    <RightPanel
      open={open}
      onClose={onClose}
      title="JOBS"
      width={380}
      icon={<JobsIcon size={14} color="var(--blue)" />}
    >
      {/* Empty state */}
      <div className={styles.emptyCard}>
        <div className={styles.emptyTitle}>+ Create your first job</div>
        <div className={styles.emptyDesc}>
          Track work items with threaded conversations. Use @mentions to loop in
          agents.
        </div>
      </div>

      {/* Status groups */}
      {STATUS_GROUPS.map((group) => (
        <div key={group.label} className={styles.statusGroup}>
          <div
            className={styles.statusHeader}
            style={{ color: group.color }}
          >
            <span className={styles.statusChevron}>&#9654;</span>
            {group.label}
          </div>
        </div>
      ))}
    </RightPanel>
  );
}
```

**File:** `apps/web/src/components/panels/JobsPanel.module.css`

```css
.emptyCard {
  border: 1px dashed var(--border);
  border-radius: 8px;
  padding: 16px;
  margin-bottom: 16px;
}

.emptyTitle {
  font-family: var(--font-body);
  font-size: 13px;
  color: var(--text-bright);
  margin-bottom: 4px;
}

.emptyDesc {
  font-family: var(--font-body);
  font-size: var(--fs-xs);
  color: var(--text-muted);
  line-height: 1.4;
}

.statusGroup {
  margin-bottom: 8px;
}

.statusHeader {
  font-family: var(--font-label);
  font-size: 11px;
  font-weight: 700;
  letter-spacing: var(--ls-lg);
  padding: 6px 0;
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  gap: 4px;
}

.statusChevron {
  font-size: 8px;
}
```

---

### Task 13.3 -- Create `apps/web/src/components/panels/RulesPanel.jsx`

Rules panel with "Remind agents" button and empty state. Width: 380px.

**File:** `apps/web/src/components/panels/RulesPanel.jsx`

```jsx
import { RightPanel } from './RightPanel';
import { RulesIcon } from '../icons/Icons';
import styles from './RulesPanel.module.css';

export function RulesPanel({ open, onClose, ruleCount = 0, maxRules = 10 }) {
  return (
    <RightPanel
      open={open}
      onClose={onClose}
      title={`RULES ${ruleCount}/${maxRules}`}
      width={380}
      icon={<RulesIcon size={14} color="var(--blue)" />}
    >
      <div className={styles.topRow}>
        <button className={styles.remindBtn}>Remind agents</button>
      </div>

      <div className={styles.emptyCard}>
        <div className={styles.emptyTitle}>+ No rules yet</div>
        <div className={styles.emptyDesc}>Tell your agents how to work</div>
      </div>

      <div className={styles.footnote}>
        New rules are sent on the next agent trigger
      </div>
    </RightPanel>
  );
}
```

**File:** `apps/web/src/components/panels/RulesPanel.module.css`

```css
.topRow {
  display: flex;
  justify-content: flex-end;
  margin-bottom: 12px;
}

.remindBtn {
  padding: 5px 12px;
  border-radius: 6px;
  background: rgba(168, 85, 247, 0.09);
  border: 1px solid rgba(168, 85, 247, 0.22);
  color: var(--purple-glow);
  font-family: var(--font-label);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: var(--ls-sm);
  cursor: pointer;
  transition: all 0.12s;
}

.remindBtn:hover {
  background: rgba(168, 85, 247, 0.16);
  border-color: rgba(168, 85, 247, 0.38);
}

.emptyCard {
  border: 1px dashed var(--border);
  border-radius: 8px;
  padding: 16px;
}

.emptyTitle {
  font-family: var(--font-body);
  font-size: 13px;
  color: var(--text-bright);
  margin-bottom: 4px;
}

.emptyDesc {
  font-family: var(--font-body);
  font-size: var(--fs-xs);
  color: var(--text-muted);
}

.footnote {
  margin-top: 16px;
  font-family: var(--font-body);
  font-size: 11px;
  color: var(--text-muted);
  font-style: italic;
}
```

---

### Task 13.4 -- Create `apps/web/src/components/panels/SettingsPanel.jsx`

Settings panel with 4 sections: Profile, Behavior, Notifications, Sounds. Features toggle switches, input fields, select dropdowns, APPLY + Cancel buttons. Width: 380px.

**File:** `apps/web/src/components/panels/SettingsPanel.jsx`

```jsx
import { useState } from 'react';
import { RightPanel } from './RightPanel';
import { BoltIcon } from '../icons/BoltIcon';
import { SectionLabel } from '../shared/SectionLabel';
import styles from './SettingsPanel.module.css';

function FieldInput({ label, defaultValue }) {
  return (
    <div className={styles.field}>
      <label className={styles.fieldLabel}>{label}</label>
      <input
        className={styles.fieldInput}
        defaultValue={defaultValue}
      />
    </div>
  );
}

function FieldSelect({ label, defaultValue, options }) {
  return (
    <div className={styles.field}>
      <label className={styles.fieldLabel}>{label}</label>
      <select className={styles.fieldSelect} defaultValue={defaultValue}>
        {options.map((o) => (
          <option key={o}>{o}</option>
        ))}
      </select>
    </div>
  );
}

function Toggle({ label, defaultOn = false }) {
  const [on, setOn] = useState(defaultOn);
  return (
    <div className={styles.toggleRow}>
      <span className={styles.toggleLabel}>{label}</span>
      <div
        className={`${styles.toggleTrack} ${on ? styles.toggleOn : ''}`}
        onClick={() => setOn(!on)}
      >
        <div className={`${styles.toggleCircle} ${on ? styles.toggleCircleOn : ''}`} />
      </div>
    </div>
  );
}

function Section({ title, children }) {
  return (
    <div className={styles.section}>
      <SectionLabel color="var(--blue)">{title}</SectionLabel>
      <div className={styles.sectionBody}>{children}</div>
    </div>
  );
}

export function SettingsPanel({ open, onClose }) {
  return (
    <RightPanel
      open={open}
      onClose={onClose}
      title="SETTINGS"
      width={380}
      icon={<BoltIcon size={14} color="var(--blue)" />}
    >
      <Section title="Profile">
        <FieldInput label="Name" defaultValue="James" />
        <div className={styles.fieldRow}>
          <FieldSelect label="Font" defaultValue="Sans" options={['Sans', 'Mono']} />
          <FieldSelect label="Contrast" defaultValue="Normal" options={['Normal', 'High']} />
        </div>
      </Section>

      <Section title="Behavior">
        <div className={styles.fieldRow}>
          <FieldInput label="Loop guard" defaultValue="4" />
          <FieldSelect
            label="Rule refresh"
            defaultValue="Every 10 triggers"
            options={['Every 10 triggers', 'Every 5']}
          />
        </div>
      </Section>

      <Section title="Notifications">
        <Toggle label="Desktop notifications" />
        <Toggle label="Sounds enabled" defaultOn />
      </Section>

      <Section title="Sounds">
        <FieldSelect
          label="Default sound"
          defaultValue="Soft Chime"
          options={['Soft Chime', 'Ping', 'None']}
        />
      </Section>

      {/* Action buttons */}
      <div className={styles.actions}>
        <button className={styles.cancelBtn} onClick={onClose}>Cancel</button>
        <button className={styles.applyBtn}>
          <div className={styles.shimmerOverlay} />
          <span className={styles.applyContent}>
            <BoltIcon size={11} color="#fff" glow={false} />
            APPLY
          </span>
        </button>
      </div>
    </RightPanel>
  );
}
```

**File:** `apps/web/src/components/panels/SettingsPanel.module.css`

```css
/* ── Section ── */
.section {
  margin-bottom: 20px;
}

.sectionBody {
  margin-top: 8px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

/* ── Fields ── */
.field {
  flex: 1;
}

.fieldLabel {
  font-family: var(--font-body);
  font-size: 11px;
  color: var(--text-muted);
  display: block;
  margin-bottom: 3px;
}

.fieldInput {
  width: 100%;
  padding: 7px 10px;
  background: var(--bg-input);
  border: 1px solid var(--border);
  border-radius: 5px;
  color: var(--text-primary);
  font-family: var(--font-body);
  font-size: var(--fs-sm);
  outline: none;
}

.fieldInput:focus {
  border-color: var(--blue);
  box-shadow: 0 0 12px rgba(0, 212, 255, 0.19);
}

.fieldSelect {
  width: 100%;
  padding: 7px 10px;
  background: var(--bg-input);
  border: 1px solid var(--border);
  border-radius: 5px;
  color: var(--text-primary);
  font-family: var(--font-body);
  font-size: var(--fs-sm);
  outline: none;
  appearance: none;
}

.fieldSelect:focus {
  border-color: var(--blue);
  box-shadow: 0 0 12px rgba(0, 212, 255, 0.19);
}

.fieldRow {
  display: flex;
  gap: 8px;
}

/* ── Toggle Switch ── */
.toggleRow {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.toggleLabel {
  font-family: var(--font-body);
  font-size: var(--fs-sm);
  color: var(--text-secondary);
}

.toggleTrack {
  width: 40px;
  height: 22px;
  border-radius: 11px;
  cursor: pointer;
  background: var(--bg-input);
  border: 1px solid var(--border);
  position: relative;
  transition: all 0.2s;
}

.toggleOn {
  background: var(--gradient);
  border-color: transparent;
  box-shadow: 0 0 14px rgba(0, 212, 255, 0.31);
}

.toggleCircle {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: #fff;
  position: absolute;
  top: 2px;
  left: 3px;
  transition: left 0.2s ease;
}

.toggleCircleOn {
  left: 21px;
  box-shadow: 0 0 8px rgba(0, 212, 255, 0.5);
}

/* ── Actions ── */
.actions {
  display: flex;
  justify-content: flex-end;
  gap: 8px;
  margin-top: 16px;
}

.cancelBtn {
  padding: 7px 16px;
  border-radius: 6px;
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text-secondary);
  font-family: var(--font-body);
  font-size: var(--fs-sm);
  cursor: pointer;
}

.cancelBtn:hover {
  border-color: var(--text-muted);
}

.applyBtn {
  padding: 7px 16px;
  border-radius: 6px;
  background: var(--gradient);
  border: none;
  color: #fff;
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 11px;
  cursor: pointer;
  box-shadow: 0 0 14px rgba(0, 212, 255, 0.25), 0 0 30px rgba(168, 85, 247, 0.16);
  position: relative;
  overflow: hidden;
}

.shimmerOverlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.12), transparent);
  background-size: 200% 100%;
  animation: shimmer 2s ease infinite;
}

.applyContent {
  position: relative;
  display: flex;
  align-items: center;
  gap: 4px;
}
```

---

## Dependency Graph

```
UI-1 (Tokens)
  |
  +-- UI-2 (Icons)
  |     |
  |     +-- UI-3 (Shared Primitives)
  |     |     |
  |     |     +-- UI-6 (Sidebar) .............. Phase 3
  |     |     +-- UI-8b (System Messages) ..... Phase 3
  |     |     +-- UI-8c (Tool Approval) ....... Phase 3
  |     |     |
  |     |     +-- UI-8a (Message Bubble) ...... Phase 2
  |     |     |     |
  |     |     |     +-- UI-9 (Chat Timeline) .. Phase 3
  |     |     |
  |     |     +-- UI-7 (Top Bar) .............. Phase 3
  |     |
  +-- UI-4 (Effects)
  |     |
  |     +-- UI-5 (App Shell)
  |           |
  |           +-- UI-6 (Sidebar)
  |           +-- UI-7 (Top Bar)
  |           +-- UI-11 (Activity Panel) ...... Phase 3
  |           +-- UI-12 (Agents Panel) ........ Phase 3
  |           +-- UI-13 (Right Panels) ........ Phase 3
```

### Recommended implementation order within Phase 3:

1. **UI-8b** (System Messages) -- no panel dependencies, standalone chat components
2. **UI-8c** (Tool Approval Cards) -- standalone chat component
3. **UI-7** (Top Bar) -- needs PendingApprovalPill from 8c
4. **UI-6** (Sidebar) -- standalone, can parallel with 8b/8c
5. **UI-9** (Chat Timeline) -- integrates 8a, 8b, 8c
6. **UI-13** (RightPanel + Jobs/Rules/Settings) -- standalone panels, RightPanel is shared wrapper
7. **UI-11** (Activity Panel) -- complex but standalone
8. **UI-12** (Agents Panel) -- most complex, standalone

PRs UI-6, UI-8b, UI-8c, and UI-13 have no cross-dependencies and can be developed in parallel.

---

## File Summary

| PR | New Files | File Paths |
|----|-----------|------------|
| UI-6 | 8 | `sidebar/Sidebar.jsx` + `.module.css`, `sidebar/ChannelList.jsx` + `.module.css`, `sidebar/RepoList.jsx` + `.module.css`, `sidebar/SidebarFooter.jsx` + `.module.css` |
| UI-7 | 4 | `topbar/TopBar.jsx` + `.module.css`, `chat/PendingApprovalPill.jsx` + `.module.css` |
| UI-8b | 4 | `chat/SystemMessage.jsx` + `.module.css`, `chat/DateDivider.jsx` + `.module.css` |
| UI-8c | 2 | `chat/ToolApprovalCard.jsx` + `.module.css` |
| UI-9 | 6 | `chat/ChatTimeline.jsx` + `.module.css`, `chat/SessionBanner.jsx` + `.module.css`, `chat/AgentThinkingStrip.jsx` + `.module.css` |
| UI-11 | 2 | `panels/ActivityPanel.jsx` + `.module.css` |
| UI-12 | 2 | `panels/AgentsPanel.jsx` + `.module.css` |
| UI-13 | 8 | `panels/RightPanel.jsx` + `.module.css`, `panels/JobsPanel.jsx` + `.module.css`, `panels/RulesPanel.jsx` + `.module.css`, `panels/SettingsPanel.jsx` + `.module.css` |

All paths relative to `apps/web/src/components/`.

**Total: 36 new files (18 JSX + 18 CSS Modules)**
